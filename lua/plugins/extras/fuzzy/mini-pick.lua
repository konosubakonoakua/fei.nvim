return {
  {
    -- TODO: try mini.pick
    -- slow when grep in $HOME
    -- fast when picking files in $HOME
    'echasnovski/mini.pick',
    version = false,
    lazy = false,
    config = true,
  },
}
